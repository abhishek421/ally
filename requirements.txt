# Requirements file
# AI Analyst Pipeline Dependencies

# Core LangGraph (for agent orchestration)
langgraph>=1.0.0

# LLM Providers
openai>=1.0.0
anthropic>=0.34.0
google-generativeai>=0.3.0

# Database
prisma>=0.11.0
psycopg2-binary>=2.9.0  # PostgreSQL driver (used by Prisma)
boto3>=1.28.0  # For DynamoDB
redis>=5.0.0  # For caching

# Vector Database & Embeddings
qdrant-client>=1.7.0  # Vector database for semantic search
sentence-transformers>=2.2.0  # For embeddings (or use OpenAI)
rank-bm25>=0.2.2  # BM25 for keyword search

# Data Validation
pydantic>=2.0.0,<3.0.0

# Utilities
python-dotenv>=1.0.0

# FastAPI and Web Server
fastapi>=0.100.0
uvicorn>=0.23.0
python-multipart>=0.0.6
aiofiles>=23.0.0  # Required for StaticFiles

# JWT Authentication (AWS Cognito)
python-jose[cryptography]>=3.3.0
requests>=2.31.0  # For fetching Cognito JWKS
